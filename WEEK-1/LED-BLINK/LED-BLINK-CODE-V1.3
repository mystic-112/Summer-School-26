/*
--------------------------------------------------
Project : LED Blink with Variable Speed Control
Author  : Abhishek Tandon
Date    : June 2026
Version : v1.3

Description:
This project demonstrates:
1. LED blinking
2. Serial communication
3. Analog input using potentiometer
4. Variable blink speed control
--------------------------------------------------
*/

int blinkCount = 0;
int potValue;
int delayTime;

void setup() {
  pinMode(13, OUTPUT);
  Serial.begin(9600);
}

void loop() {

  potValue = analogRead(A0);

  delayTime = map(potValue, 0, 1023, 100, 1000);

  digitalWrite(13, HIGH);
  delay(delayTime);

  digitalWrite(13, LOW);
  delay(delayTime);

  blinkCount++;

  Serial.print("Blink Count: ");
  Serial.print(blinkCount);

  Serial.print(" | Delay: ");
  Serial.print(delayTime);
  Serial.println(" ms");
}
